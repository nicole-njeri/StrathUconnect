import 'package:flutter/material.dart';

class CampusUpdatesScreen extends StatelessWidget {
  const CampusUpdatesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Campus Updates')),
      body: const Center(child: Text('Campus Updates feature coming soon!')),
    );
  }
}
